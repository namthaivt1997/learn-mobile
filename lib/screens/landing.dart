import 'package:flutter/material.dart';
import 'package:x_system/screens/login.dart';

class LandingScreen extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: LoginScreen(),

    );
  }

}
